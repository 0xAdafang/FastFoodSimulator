use std::time::Duration;

pub const ORDER_TAKER_INTERVAL: Duration = Duration::from_secs(3);
pub const COOK_PREPARATION_TIME: Duration = Duration::from_secs(5);
pub const SERVER_DELIVERY_TIME: Duration = Duration::from_secs(2);
